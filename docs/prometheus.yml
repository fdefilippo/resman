# Validate with: promtool check config docs/prometheus.yml
# Smoke test: curl -fsS http://localhost:1974/metrics | grep '^resman_'
scrape_configs:
  - job_name: 'resman'
    static_configs:
      - targets: ['192.168.1.7:1974'] # Replace with your program's IP and port
